import React from 'react';

const NotFound = () => {
    return (
        <div className='not-found small-header'>
            Sorry, but I don't found page with this URL.Please, Trying go into menu.
        </div>
    );
};

export default NotFound;